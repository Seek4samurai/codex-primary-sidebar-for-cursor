{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Codex Cursor Sidebar",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}"
      ]
    }
  ]
}